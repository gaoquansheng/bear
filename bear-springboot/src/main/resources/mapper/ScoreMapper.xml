<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bear.bearspringboot.mapper.ScoreMapper">

    <insert id="addScore" parameterType="Score">
        insert into score (planId, score, userTel, indexId, reviewTime) values (#{planId}, #{score}, #{userTel}, #{indexId}, #{reviewTime})
    </insert>
</mapper>
